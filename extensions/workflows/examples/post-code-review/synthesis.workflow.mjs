export const meta = {
  name: "post-code-review/synthesis",
  description: "Verify review evidence and publish the final code-shape decision.",
  profile: "standard",
};

export default async function runWorkflow(dsl, input) {
  await dsl.agent(
    `Synthesize a post-code review for this semantic target and intent:
${input}

Read review-scope.md, review-boundaries.md, review-simplicity.md, review-contracts.md, review-style.md, and review-necessity.md from the runtime-injected shared workflow output directory. Treat the accepted included/excluded/local-only delivery scope and explicit implementation boundaries in review-scope.md, the accepted task, and its decisions as authoritative: do not turn an explicitly excluded or local-only absence from public/package/tracked surfaces into a finding, inspect named local or ignored evidence when scope points there, and reject any lane finding or required action that contradicts an accepted boundary. Read the live source and the relevant consumers, callers, tests, documentation, specifications, decisions, repository guidance, style conventions, and Git/source evidence needed to verify the scope. Independently verify every claim, remove duplicate, unsupported, stale, or unnecessary findings, and grade each retained finding for correctness, evidence, consumer impact, duplication, actionability, and user value.

For a commit, range, diff, or PR target, independently verify every retained finding,
positive evidence, accepted boundary, and NO_ACTION architecture claim against the exact
reviewed target tree and diff. Before saying a config key, module allowlist, owner, or
dependency exception existed at the target, read it from the frozen target bytes and check
the owning AGENTS.md, package README, ADR, and executable policy for contradiction. A
live-only descendant policy is current context, not target evidence. Reject or reframe a
decision-critical claim when its cited key or rule is absent from the target tree; do not
let agreement among scope and lane reports substitute for that readback.

The necessity challenge is an admission gate, not another vote. Retain a lane finding only when review-necessity.md assigned RETAIN or REFRAME and supplied evidence-backed answers to all four questions: the proven failure or violated contract, the owner of the guarantee, whether the fix duplicates another component or external dependency, and why the action is the simplest net improvement. Synthesis may narrow or reject an admitted finding after live verification, but it must not restore a proposal that the necessity challenge rejected or could not evaluate. When an external provider is explicitly trusted for field type/value guarantees and the local component neither claims a stricter contract nor owns a transforming invariant, absence of repeat local validation is an accepted responsibility boundary, not a defect. A new controller, parser, validator, ledger, structured protocol, retry layer, or other machinery is justified only when the admitted finding proves its owner, supported-consumer impact, non-duplication, and net simplicity.

Do not treat current documentation or tests as proof that a dependency belongs to its present component: they prove the dependency exists and is exercised. A narrower component's explicit responsibility, dependency direction, supported consumers, and broader integration ownership still decide whether the coupling violates a contract. Moving a check from the wrong narrow-layer owner into the existing integration owner is not duplication when the old check is removed. Retain such an ownership finding without requiring a runtime crash when the current coupling itself makes a supported narrower-layer consumer depend on an unrelated broader runtime.

Apply two evidence paths instead of reducing every review question to a failing consumer. A behavioral or data-contract defect must name the supported consumer or breached runtime boundary that makes the value authoritative. A current code-shape defect is independently proven when the reviewed change introduces or materially worsens consumerless or dead surface, fake configurability, duplicate ownership of one invariant, a wrapper or seam without policy or production variation, stale derived documentation changed by the review target, a misleading behavior description, responsibility drift, or another open delete, rewrite, inline, or move-owner disposition. That second class does not require a runtime crash. Distinguish duplicate descriptions that must stay equal and fail silently on drift from coincidental repetition that has no shared invariant.

Human-facing guidance and an agent's ordinary prose are not a machine-authoritative success protocol unless a named supported consumer parses or trusts them as one; the mere possibility that an agent can make a false claim is an evidence limitation, not a current consumer defect. When stronger future enforcement would be useful but the reviewed change creates no current behavioral or code-shape defect, classify it as a residual risk or follow-up enhancement. Retain every admitted and independently verified material defect with concrete consumer or maintenance impact: do not cap retained findings by count, severity headline, or a selected “top three”; completeness outranks brevity. For a commit, commit range, diff, or PR target, retain a defect only when the reviewed change introduced it, materially worsened it, or newly made it reachable; classify an unrelated pre-existing defect as out of scope or a follow-up enhancement even when it is real. Treat speculative future-only hardening as optional, but do not downgrade confirmed current duplication, dead surface, fake parameters, stale derived documentation, or owner drift merely because execution still succeeds. Treat an explicitly documented caller-owned launch precondition as a contract boundary rather than a missing workflow-side validator when every supported launch form carries that precondition and focused proof demonstrates both the safe path and the unsafe negative case. For every removed, renamed, or canonicalized surface, independently close the change across unchanged consumers, allowlisted consumers, and operator commands; treat an allowlist as evidence to inspect, never as an executable contract. Required actions must cover the full proven dependency chain without duplicating a trusted owner. Classify taste without a project rule or concrete maintenance risk as NO_ACTION polish, not a finding.

Make the code-shape decision explicit and mechanically readable in prose. Assign exactly one top-level decision: READY when this review gate is satisfied and no source change is warranted; READY_WITH_RECOMMENDATIONS when this review gate is satisfied and only evidence-backed optional work outside the current change's acceptance contract remains; CHANGES_REQUIRED when at least one current behavioral, contract, documentation, ownership, style-contract, or code-shape defect must be fixed and re-reviewed; BLOCKED when evidence is insufficient for a trustworthy decision. This is not a final merge or QA verdict.

For every retained finding, enhancement, rejected proposal, or accepted boundary, preserve its lane question id and assign exactly one Action: REQUIRED, RECOMMENDED, or NO_ACTION. REQUIRED means the reviewed change introduced or materially worsened a proven current defect, including an open delete, rewrite, inline, documentation repair, or owner move. RECOMMENDED means evidence supports useful non-blocking work, but it is outside the current change's acceptance contract or requires a future product decision. NO_ACTION means the proposal is taste-only, rejected, duplicated by an existing owner, out of scope, already satisfied, or an accepted responsibility boundary. Also assign Impact: high, medium, or low independently of Action. Low impact changes verification priority; it does not make a proven current defect optional. The top-level decision must agree with the item actions: any REQUIRED item means CHANGES_REQUIRED; recommendations without required work mean READY_WITH_RECOMMENDATIONS; no required or recommended work means READY. Every material lane question id must appear exactly once with one closed disposition; a missing, duplicate, or open id returns BLOCKED rather than a favorable verdict.

For every REQUIRED or RECOMMENDED item, include one small illustrative fix snippet when a truthful localized example can clarify the intended change. Use the project's language and live names, keep it narrowly focused, and label it explicitly as illustrative guidance that must be adapted to the live code rather than copied as a patch. Do not include a snippet when the change is inherently cross-cutting, when evidence cannot support a correct example, or for any NO_ACTION item. A snippet never substitutes for path:line evidence, ownership, a complete action, tests, or the necessity proof.

Perform synthesis within this assigned Pi session. Do not invoke or delegate to another agent, saved workflow, Fusion, Claude, Codex, or any other outside model/session through a tool or shell command. Do not modify project source or any project file other than the assigned post-code-review.md. Every agent-caused filesystem write, including tool-generated caches, bytecode, indexes, reports, fixtures, logs, build/state/evidence directories, and lock/dependency metadata, must stay under the runtime-injected workflow workspace; do not run a command that writes elsewhere. If a useful check has implicit output, redirect all output and cache under the workspace or use a no-cache/read-only mode; otherwise record it as an evidence limit. Write no other artifact. Finish only after writing the complete replacement post-code-review.md in the shared workflow output directory; do not append or leave a partial report. Include the semantic scope and intent, the top-level code-shape decision, retained findings with Action, Impact, path:line evidence, grades, simplest fix and appropriate illustrative snippets, top risks, positive evidence, recommendations, NO_ACTION decisions with reasons, rejected/duplicate/unsupported claims, evidence gaps, and limits. End with a short implementation handoff that names the action threshold and gives a copyable \`/workflows run implement --output-dir <this-workspace> ...\` command when REQUIRED or explicitly selected RECOMMENDED work exists; otherwise state that implementation would intentionally be a no-op.`,
    { modelRole: "smol:xhigh", requireModelRole: true },
  );
  return dsl.publishPrimaryFile("post-code-review.md");
}

# Prettier reads .gitignore but not .git/info/exclude, so owner-local directories
# hidden only through the exclude file must be listed here. The git-ignored
# directories below are repeated for tools that read only this file.
# `dist/` is intentionally absent: .gitignore carves tracked files out of it.
.reports/
.tasks/
.locus/
node_modules/
